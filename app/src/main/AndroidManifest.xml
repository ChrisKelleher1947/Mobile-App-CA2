<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".main.MainApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/my_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CA1ChrisKelleherMobileAppDev"
        tools:targetApi="34">

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/MAPS_API_KEY" />

        <!-- Launcher Activity -->
        <activity
            android:name=".views.auth.SignInView"
            android:exported="true"
            android:theme="@style/Theme.PetCare.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Other activities -->
        <activity
            android:name=".views.map.MapView"
            android:label="@string/title_activity_map"
            android:exported="false" />

        <activity android:name=".views.addPet.PetCareView" />
        <activity android:name=".views.petdetails.PetDetailsView" />
        <activity android:name=".views.petlist.PetRecordListView" />
        <activity android:name=".views.home.HomeView" />
        <activity android:name=".views.settings.SettingsView"/>

    </application>

</manifest>
